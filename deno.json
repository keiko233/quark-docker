{
  "tasks": {
    "dev": "deno run --watch main.ts",
    "download-client": "deno run -A download-client.ts",
    "postinstall": "deno run -A playwright install --with-deps chromium"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "playwright": "npm:playwright@^1.59.1"
  }
}
